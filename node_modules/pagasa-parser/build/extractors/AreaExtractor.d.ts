import { Area } from "../typedefs/Area";
export default class AreaExtractor {
    private readonly areaString;
    private get currentChar();
    private currentPos;
    private readonly subroutines;
    constructor(areaString: string);
    /**
     * Attempts to fix possible parsing issues or otherwise unclean strings
     * from a pagasa-parser data source.
     * @param areaString
     */
    attemptRepair(areaString: string): string;
    extractAreas(): Area[];
    private shift;
    private peek;
    private match;
    private peekTerm;
    private nextTerm;
    private nextLocation;
    private extractPartitionComponents;
    private extractPartitioned;
    private extractRest;
    private extractMainland;
    private extractLocation;
    private extractIslands;
}
